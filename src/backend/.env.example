# Backend env template — copy to .env and fill in values
PORT=3001
CORS_ORIGIN=http://localhost:5173

# PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/pixelcartel_dev

# Auth (fill when implementing)
JWT_SECRET=changeme
